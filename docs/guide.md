# 사용 안내 — 언제 무엇을 쓰나

이 저장소의 다른 문서는 **무엇을 만들었나**를 설명한다. 이 문서는 **내가 언제 무엇을 쓰나**를
설명한다. 하네스를 직접 만들지 않은 사람(또는 만들고 나서 잊어버린 사람)이 읽는 문서다.

- 설계·구조 → [overview.html](overview.html)
- 훅 내부 동작 → [self-improvement-hooks.md](self-improvement-hooks.md)
- Codex 훅 제약 → [codex-hooks.md](codex-hooks.md)

---

## 0. 한 문단 요약

이 하네스는 **작업이 끊길 때 잃어버리는 것**을 줄인다. 세션이 끝나거나, 툴을 바꾸거나,
머신이 바뀌거나, 며칠 지나 돌아왔을 때 "내가 뭘 하고 있었지"와 "이거 전에도 겪었는데"를
매번 다시 겪지 않게 한다.

세 가지를 한다.

| | 무엇을 |
|---|---|
| **이어받기** | 하던 작업 상태를 넘긴다 (`handoff-*`, `fw`, `history`) |
| **기억** | 배운 것을 다음 세션이 자동으로 보게 만든다 (메모리 + 훅) |
| **점검** | 놓치기 쉬운 뒷정리·검증을 대신 찾아준다 (`merge-cleanup`, `stale-scan` 등) |

---

## 1. 상황별 — 이럴 때 이걸 쓴다

### 작업을 넘길 때 / 이어받을 때

| 상황 | 쓸 것 |
|---|---|
| 오늘 여기까지, 내일 이어서 | `/handoff-save` |
| 다른 머신·사람이 이어받아야 함 | `/handoff-save` → 커밋 |
| 남이(또는 어제의 내가) 남긴 걸 이어받기 | `/handoff-load` |
| **저장 안 하고 세션이 끝나버림** | `/fw` |
| Claude·Codex 오가며 작업했음 | `/fw-both` |
| "그때 그 작업이 어느 세션이었지?" | `/history` |

**`handoff` 와 `fw` 의 차이가 핵심이다.**

- `handoff-save/load` = **사람이 명시적으로 저장한** 인계 파일. 커밋되므로 **다른 머신·다른 사람**도 읽는다. 정본이다.
- `fw` = 저장을 **안 했을 때의 구조 수단**. 세션 로그(Claude `.jsonl` / Codex rollout)에서 자동 복원한다. **같은 머신에서만** 되고, 커밋되지 않는다.

즉 `fw` 는 "아 저장 안 했네" 할 때 쓰는 것이다. 평소엔 `handoff-save` 를 쓰는 게 맞다.

`/history` 는 복원하지 않는다. **찾기 전용**이다 — 세션 목록을 훑고 검색해서, 고른 세션을
`fw` 에 넘길 명령을 출력해준다.

### PR 작업 중

| 상황 | 쓸 것 |
|---|---|
| PR 머지·close 한 뒤 뒷정리 | `/merge-cleanup` |
| 리뷰가 2라운드 이상 이어짐 | `/review-ledger` |
| 새로 추가한 테스트가 진짜 회귀를 잡는지 | `/verify-regression` |
| `prettier --write` 돌리기 전 (JS/TS 프로젝트) | `/prettier-guard` |

### 작업이 끝난 뒤

| 상황 | 쓸 것 |
|---|---|
| 이번에 지적받은 걸 규칙으로 남길지 검토 | `/feedback-review` |
| 배운 것을 메모리에 영속화 | `/memory-update` |
| 오래된 이슈 백로그 정리 | `/stale-scan` |

---

## 2. 스킬 12개 — 하나씩

### 자주 쓰게 되는 것

**`/handoff-save`** — 지금 상태를 커밋 가능한 파일로 저장한다. 브랜치, 무엇을 하다 말았는지,
다음 액션, 열린 리뷰 finding 까지 담는다.

```
/handoff-save 실거래 클라이언트 단일화 80% 완료
```

한 줄 요약은 선택이지만 넣는 게 좋다. 나중에 목록에서 이것만 보고 고른다.

**`/handoff-load`** — 커밋된 핸드오프를 1순위로 읽고 **현재 git 상태와 대조**한다.
핸드오프가 오래됐으면 그 사실을 알려준다. 상태 보고까지가 기본이고, 검증·빌드·git 조작은
따로 시켜야 한다.

**`/fw`** — 저장 안 했어도 세션 로그에서 복원한다. 기본값이 **반대 툴**이다(Claude 에서 쓰면
Codex 로그를 본다). 현재 세션이 자기 자신을 "직전 작업"으로 고르는 걸 막기 위해서다.

**`/memory-update`** — 이번 세션에서 배운 것을 메모리로 승격한다. 개인/공유 tier 를 나누고,
공유 tier 는 커밋이 필요하다. `_pending` 초안이 있으면 같이 검토한다.

**`/merge-cleanup`** — 머지 후 정리 후보를 **advisory 로만** 보고한다. 직접 지우지 않는다.
로컬 main 동기화, 병합된 브랜치, 닫아도 될 이슈, worktree, untracked 잔여물.

> ⚠️ 알려진 버그(#78): 이 저장소처럼 **squash merge** 를 쓰면 `git branch --merged` 가
> 아무것도 못 잡아서 "정리할 것 0개"로 오탐한다. 원격 브랜치 섹션만 PR API 를 써서 제대로
> 잡는다. 로컬 브랜치 결과는 아직 믿지 말 것.

### 상황이 와야 쓰는 것

**`/review-ledger`** — PR 리뷰가 여러 라운드로 갈 때, finding 을 기억에 맡기지 않고 원장으로
관리한다. **"기존 open finding 검증 먼저, 그다음 신규 탐색"** 순서를 강제한다.

```
agent-review-ledger init --pr 123
```

원장은 `.claude/.cache/review-ledger/pr-123.json` 에 저장되고 커밋되지 않는다.
`handoff-save` 가 열린 finding 을 핸드오프에 자동으로 포함한다.

리뷰가 1라운드로 끝나면 쓸 일이 없다. **3라운드쯤 가면서 "아까 그 지적 반영했나?"가 헷갈릴 때**
값어치가 나온다.

**`/verify-regression`** — 새 테스트가 **수정 전 코드에서 실제로 실패하는지** 임시 worktree 로
검증한다. "테스트를 추가했다"와 "그 테스트가 진짜 버그를 잡는다"는 다르다. 수정 후 코드에서만
돌려보면 후자를 증명하지 못한다.

버그 픽스 PR 에서 "이 테스트가 회귀를 재현하나?"를 확인할 때 쓴다.

**`/stale-scan`** — 오래 열린 이슈 중 **이미 해결됐을 후보**를 연결된 merged PR 근거로 찾는다.
자동으로 닫지 않는다.

```
agent-stale --repo foxyberry/agent-harness --min-age-days 30
```

> ⚠️ 알려진 한계(#79): 이슈 본문에 단순히 **언급**된 것을 해결로 오판할 수 있다. 후속 이슈를
> 링크할수록 오탐이 늘어난다.

**`/prettier-guard`** — `prettier --write` 가 **원래부터 지저분했던** 파일까지 통째로 재정렬하는
걸 막는다. 변경 파일을 먼저 분류해서 안전한 것만 제안한다.

**이 저장소에는 해당 없다**(Python 프로젝트). JS/TS 프로젝트에서 쓰는 도구다.

**`/history`** — 세션 로그를 시간순으로 조회·검색한다.

```
/history --grep 공시지가 --since 7d
```

복원은 안 한다. 찾아서 `fw` 명령을 출력해줄 뿐이다.

**`/fw-both`** — Claude·Codex 로그를 **한 번에** 본다. 여러 툴에 작업이 흩어졌을 때 합쳐서
이어받는다. 현재 툴의 live 세션만 배제한다.

**`/feedback-review`** — 이번 세션에서 받은 지적을 규칙·스킬로 승격할지 검토한다.

> ⚠️ 알려진 한계: **현재 세션만** 본다. `_pending` 초안도 안 읽는다(그건 `/memory-update` 몫).
> 반대 툴에서 한 작업은 재료가 없다(#81).

---

## 3. 안 써본 스킬이 왜 안 써졌나

정직하게 따져보면 이유가 세 가지다. **만든 게 잘못이라서가 아니다.**

### (1) 이 저장소에서는 상황이 안 온다

| 스킬 | 필요한 상황 | agent-harness 에서는 |
|---|---|---|
| `prettier-guard` | JS/TS + prettier | Python 프로젝트라 **해당 없음** |
| `verify-regression` | 버그 픽스 + 회귀 테스트 | 주로 문서·훅 작업이라 기회가 적음 |
| `review-ledger` | 3라운드 이상 리뷰 | 대부분 1~2라운드에 끝남 |
| `stale-scan` | 오래된 이슈 백로그 | 저장소가 젊고, 정리를 수동으로 함 |

**이 도구들은 원래 tutti-dpnc 같은 실제 제품 저장소에서 쓰라고 만든 것이다.**
하네스는 여기서 만들지만 쓰이는 곳은 다른 저장소다. 그러니 "여기서 안 썼다"는 정상이다.

### (2) 있는 줄 몰라서

`history`, `fw-both` 가 여기 해당한다. 상황은 여러 번 왔는데(세션을 여러 개 오갔다) 떠올리지
못했다. **이건 도구 문제가 아니라 발견 가능성 문제다.** 이 문서가 그걸 메우려는 것이다.

### (3) 자동이라 쓴 줄 모르는 것

훅은 **명령이 아니다.** 세션 시작·파일 편집 전후·머지 시에 알아서 돈다. 메모리 인덱스 주입,
편집 전 관련 메모리 표시, PR 머지 후 회고 재촉이 전부 훅이다. "안 썼다"가 아니라 **이미 계속
쓰고 있었다.**

**단, 이건 Claude 이야기다.** Claude 에서는 4개가 전부 돈다. **Codex 에서는 지금
`project-memory-index` 하나만 돌고**, 그것도 훅을 신뢰해야 실행된다. Codex 에서 파일을 고칠 때
메모리가 안 뜨거나 품질 경고가 없는 건 고장이 아니라 **아직 이식 전**이라서다(#85).

---

## 4. 메모리 — 이게 이 하네스의 핵심이다

### 두 층

| | 위치 | 커밋 | 누가 읽나 |
|---|---|---|---|
| **개인** | `~/.claude/projects/<프로젝트>/memory/` | ✗ | Claude Code, 이 머신만 |
| **공유** | `<repo>/.claude/memory/` | ✓ | Claude + Codex 모두 |

**개인**은 "나와 일하는 방식"이다 — 보고를 어떻게 할지, 언제 물어볼지, 커밋 서명 규칙 같은 것.
**공유**는 "이 프로젝트의 사실·결정"이다 — 왜 이렇게 설계했는지, 무엇을 하면 안 되는지.

애매하면 물어봐야 한다. 개인 취향인지 팀 규칙인지가 갈림길이다.

### 어떻게 자동으로 뜨나

```
세션 시작  → INDEX.md 목록이 통째로 주입 (project-memory-index)   ← Claude·Codex 둘 다
파일 편집 전 → routes.json 이 가리키는 메모리 본문이 주입 (memory-search)  ← Claude 만
파일 편집 후 → reflection-rules.json 정규식으로 품질 경고 (reflection)     ← Claude 만
PR 머지 후  → 회고 재촉 (pr-merge-reflect)                              ← Claude 만
```

**Codex 는 첫 줄 하나만 돈다.** 나머지 셋은 이식 전이다(#85).

**`routes.json` 이 없으면 두 번째가 아예 안 돈다.** 메모리를 만들어놓고 route 를 안 걸면
"세션 시작 시 목록에 한 줄 보이는" 정도로만 존재한다. 메모리를 추가할 때 route 도 같이
거는 이유가 이것이다.

### description 이 중요한 이유

`memory-search` 훅은 파일 **전문**을 주입하므로 `description` 은 안 쓴다.
`description` 이 쓰이는 곳은 **`INDEX.md` 한 줄뿐**이고, 그게 세션 시작 때 보이는 전부다.

그래서 한 줄이 격언이면 안 된다. 통과 기준은 하나다 — **"이 한 줄만 보고 내가 파일을 열까?"**

```
❌ 복구용 기록은 복구 대상 바깥에          ← 언제 쓰는 규칙인지 안 보임
✅ 캐시·alias 저장 위치: worktree 안에 두지 말 것, .git 공통 디렉터리에
```

### governance

공유 메모리는 `_pending → 사람 승인 → committed` 를 거친다. 자동 생성된 교훈이 검토 없이
박히지 않게 하는 장치다.

> 참고: `_pending` 을 채우는 자동 회고는 **기본 꺼져 있다**(`HARNESS_AUTO_REFLECT=1` opt-in).
> 설치만으로 백그라운드 LLM 잡이 뜨지 않게 한 것이다. 그래서 지금까지 `_pending` 은 한 번도
> 생긴 적이 없고, 메모리는 전부 `/memory-update` 로 수동 승격했다.

---

## 5. 고칠 때 알아야 할 것

`core/` 가 정본이고 `plugins/` 는 **생성물**이다.

```
core/ 수정 → ./build.sh → plugins/harness (Claude) + plugins/codex (Codex)
```

**어댑터를 직접 고치면 다음 빌드에서 덮인다.** CI 가 build 후 `git diff` 로 drift 를 검사하므로
빌드 안 돌리고 커밋하면 실패한다.

문서를 고쳐도 동작은 안 바뀐다. 예를 들어 `docs/codex-hooks.md` 는 설명일 뿐이고, Codex 에
훅을 추가하려면 **`build.sh`** 를 고쳐야 한다.

---

## 6. 지금 상태 (2026-08-11)

| | Claude | Codex |
|---|---|---|
| 스킬 | 12개 | 12개 |
| 훅 | 4개 전부 | **1개** (`project-memory-index`) |
| 호출 방식 | 슬래시 커맨드 | description 매칭 |
| 훅 신뢰 | 불필요 | **필요** — 안 하면 무음으로 안 돎 |

Codex 훅 나머지 3개는 이식 가능한 것까지 확인됐고 작업이 남아 있다(#85).

### 알려진 버그

- **#78** `merge-cleanup` 이 squash merge 저장소에서 로컬 브랜치를 0개로 오탐
- **#79** `stale-scan` 이 단순 언급을 해결로 오판
- **#81** `/feedback-review` 가 `_pending` 과 과거 세션을 안 봄
