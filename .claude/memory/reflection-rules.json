{
  "//": "reflection hook data (agent-harness dogfooding itself). Quality rules for Python hooks. The built-in TODO/FIXME rule always applies.",
  "rules": [
    {
      "glob": "*.py",
      "regex": "except\\s*:",
      "message": "bare except in {count} place(s) — hooks should fail open with `except Exception` so they never block the session"
    },
    {
      "glob": "*.py",
      "regex": "shell\\s*=\\s*True",
      "message": "shell=True in {count} place(s) — shell injection risk, consider passing an argument list instead"
    }
  ]
}
