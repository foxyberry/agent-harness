# Python
__pycache__/
*.pyc

# OS
.DS_Store

# Local experiments / install output
*.local

# Self-improvement hook runtime output (never commit — governance and per-machine state)
.claude/memory/_pending/
.claude/.cache/
# Record of discarded retrospective drafts. This is the **personal tier** — a list of lessons
# deliberately not kept is really a record of work habits and mistakes, and the memory tier rule
# in AGENTS.md says that does not get committed (this repository is public).
.claude/memory/_rejected.md
